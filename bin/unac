#!/usr/bin/perl -CA
BEGIN {
    *arg = @ARGV ? \&CORE::shift : \&CORE::readline;
}
use utf8;
use open qw( :encoding(UTF-8) :std );

use Unicode::Normalize;

while ($_ = arg) {
    chomp;
    print NFD($_) =~ s/[\p{Mn}]//rg, "\n";
}

