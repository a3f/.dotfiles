#!/usr/bin/perl

use Digest::Adler32 qw(adler32);


while (<>) {
    my $a32 = Digest::Adler32->new;
    $a32->add($_);
    $digest = unpack 'H*', pack 'N', unpack 'L', $a32->digest;
    print $digest, "\n";
}
